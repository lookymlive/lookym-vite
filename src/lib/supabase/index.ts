// Exportamos todo desde el archivo de configuración
export * from "./config";
export * from "./types";

// Este archivo sirve como punto de entrada único para todas las funcionalidades de Supabase
// Importa este archivo en lugar de los archivos individuales para acceder a todas las funcionalidades
// Ejemplo: import { supabase, authConfig } from '@/lib/supabase';
